# Ya no se necesita VITE_API_URL — todo se sirve desde el mismo origen
# Configurar proxy reverso: /api → backend, /photos → backend
# IMPORTANTE: En el nginx de visitante.amp.gob.pa, usar location ^~ /photos/
# para que tenga prioridad sobre los bloques regex de archivos estáticos
